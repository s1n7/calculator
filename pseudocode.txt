variable operand a
variable operand b 
variable operator o

addEventListener to button (click, operate(e))

if e == number
    a = a + button value //concatenate button value to a
    display a

if button == operator 
    o = button 


